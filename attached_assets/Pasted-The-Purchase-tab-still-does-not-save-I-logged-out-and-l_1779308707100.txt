The Purchase tab still does not save.

I logged out and logged back in, and the Purchase tab address/numbers were gone again.

Do not touch Insurance.
Do not touch Refinance.
Do not redesign anything.
Only debug and fix Purchase persistence.

This is now specifically a database persistence issue. I need you to prove where the Purchase data is failing:

1. Is the Purchase record actually being inserted/upserted into Supabase?
2. If yes, is it being saved with the correct user ID and flow/type?
3. If yes, is the Purchase dashboard query loading it after login?
4. If yes, is the frontend filtering it out?
5. If no, why is the save/upsert not happening?

Do not assume auto-save works. Verify it.

Required debugging:
Add temporary console logs and/or visible dev logs for Purchase only:

When address is selected:
- selected Google formatted address
- generated purchase scenario/property ID
- authenticated user ID
- normalized property key
- flow/type value
- full object being saved

When saving to Supabase:
- exact table name
- exact insert/upsert payload
- Supabase response
- Supabase error, if any

When dashboard reloads after login:
- authenticated user ID
- exact table being queried
- exact filters used
- raw records returned from Supabase
- count of purchase records returned
- final records rendered in Purchase tab

Likely root causes to inspect:
- Purchase is only saving to local state.
- Purchase save function is never called after address selection.
- Purchase save function is called before auth user is ready, so `user_id` is null.
- Purchase save function has a Supabase error that is being swallowed.
- Purchase record is saved with the wrong `flow_type`, `type`, `category`, or `scenario_type`.
- Purchase dashboard query is filtering for a value that does not match the saved value.
- Purchase dashboard query is requiring fields that purchase records do not have yet.
- Purchase dashboard only loads completed estimates instead of address-only estimates.
- Purchase local state is empty after login and overwrites the loaded Supabase records.
- Purchase records are saved under a temporary guest/session ID instead of the authenticated user ID.
- Purchase records are saved to a different table than the dashboard reads from.
- RLS is blocking insert or select for purchase records.

Fix requirements:
1. When a logged-in user selects a valid address in Purchase Property Estimate, immediately save a durable record to Supabase.
2. The saved Purchase record must include:
   - stable scenario/property ID
   - authenticated `user_id`
   - full formatted address
   - normalized property key
   - flow/type/category = purchase
   - current estimate numbers, even if they are default values
   - created_at
   - updated_at
3. This must save even if:
   - Zillow has not loaded yet
   - questionnaire is incomplete
   - only the address exists
   - no documents are uploaded
4. When Zillow/cache data returns, update the same Purchase record by stable ID.
5. When user edits purchase fields, update the same Purchase record by stable ID.
6. On login/dashboard load, query Supabase after auth is ready and load all Purchase records for that user.
7. Do not let empty local state overwrite Supabase-loaded Purchase records.
8. Do not require a manual Save to Dashboard button.
9. Do not make Purchase depend on Refinance documents or mortgage statements.
10. Purchase records should only disappear if the user deletes them.

Important implementation direction:
Use the working Refinance persistence pattern as the model.

If Refinance is saved in table X with fields A/B/C and loaded with query Y, then make Purchase use the same durable pattern, but with type/flow = purchase.

Do not create a new architecture unless absolutely necessary.

Acceptance test:
1. Log in.
2. Add a Purchase property address.
3. Confirm the console shows a Supabase insert/upsert success.
4. Confirm the Purchase tab shows the property.
5. Refresh the page.
6. Confirm the Purchase tab still shows the property.
7. Log out.
8. Log back in.
9. Confirm the Purchase dashboard query returns the saved record from Supabase.
10. Confirm the Purchase tab renders the saved record.
11. Add a second Purchase property.
12. Confirm both Purchase properties persist after refresh and logout/login.
13. Confirm Zillow/cache updates update the same records, not duplicates.
14. Confirm Insurance view is unchanged.
15. Confirm Refinance behavior is unchanged.

Before making changes, tell me:
- Where Purchase is currently being saved
- Whether Supabase is receiving the save request
- Whether Supabase returns an error
- Where Purchase dashboard is currently loading from
- Why the records disappear after logout/login

After changes, tell me:
- Exact table used
- Exact fields used for Purchase persistence
- Exact save function changed
- Exact load/query function changed
- Why this now persists after logout/login