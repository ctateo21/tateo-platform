The credit score below 720 → FHA rule is still not working.

I changed the credit score to 680 and the app still stayed Conventional.

At this point, do not keep trying generic recommendation fixes. The issue is likely that the UI is reading from a different loan type field than the field being updated, or another effect is changing it back to Conventional after the credit score logic runs.

Hard rule:
Anytime the user’s credit score is below 720, the selected loan type shown to the user must become FHA.

No exceptions for now.

Do not let manual override block this.
Do not let DPA logic block this.
Do not let Page 3/Page 4 state mismatch block this.
Do not leave the visible loan type as Conventional when score is 680.

Required debugging:
Trace exactly what happens when the credit score changes from 780 to 680.

Add temporary console logs in every place that reads or writes loan type:

- credit score input handler
- loan recommendation function
- loan type selector component
- Page 3 Purchase Details state
- Page 4 See My Estimate state
- purchase scenario update function
- FHA DPA logic
- any effect/useEffect that sets Conventional
- any effect/useEffect that recalculates recommended loan type
- any auto-save/load function that writes loan type back into state

Log:
- previous credit score
- new credit score
- previous visible loan type
- loan type immediately after credit score handler
- loan type after recommendation function
- loan type after DPA logic
- loan type after recalculation
- loan type after save/load
- final visible loan type rendered in UI
- which field the UI is actually displaying

Possible loan type fields to inspect and sync:
- `loanType`
- `loan_type`
- `selectedLoanType`
- `recommendedLoanType`
- `scenario.loanType`
- `scenario.loan_type`
- `purchaseDetails.loanType`
- `purchaseDetails.loan_type`
- `mortgage.loanType`
- `estimate.loanType`
- Page 3 local state
- Page 4 local state
- form state
- dashboard scenario state

Fix requirement:
There must be one single source of truth for Purchase loan type.

If multiple loan type fields currently exist, either:
1. Refactor to one source of truth, or
2. Temporarily sync all fields when credit score falls below 720.

When credit score changes and score < 720, run a direct forced update:

- set visible selected loan type to FHA
- set scenario loan type to FHA
- set purchase details loan type to FHA
- set estimate loan type to FHA
- set saved loan type to FHA
- set down payment percent to 3.5
- set `uses_dpa = false`
- set `dpa_type = null`
- recalculate using FHA pricing
- auto-save the updated scenario

Important:
This logic should run after the credit score changes and before the UI renders the next estimate.

If another function later changes the loan type back to Conventional, find it and fix it.

Likely root cause:
A later effect is probably doing one of these:
- defaulting loan type back to Conventional
- using an old saved scenario value
- using a recommended loan type separate from the selected loan type
- treating Conventional as default when DPA is off
- reading Page 3 state while Page 4 state was updated
- loading stale local/Supabase state after the credit score handler runs

Temporary direct implementation:
In the credit score change handler or centralized purchase scenario update function, add:

```js
if (Number(newCreditScore) < 720) {
  updatePurchaseScenario({
    loan_type: "fha",
    loanType: "fha",
    selectedLoanType: "fha",
    recommendedLoanType: "fha",
    down_payment_percent: 3.5,
    uses_dpa: false,
    dpa_type: null
  });
}