There is a persistence bug in the logged-in dashboard.

Current issue:
The Refinance tab is saving property addresses correctly. When I leave and come back, the refinance addresses are still there.

But the Purchase tab is inconsistent. When I enter purchase property estimates and come back later, the addresses/properties are not always there.

Expected behavior:
Purchase property estimates should persist exactly like refinance property records do.

If a logged-in user adds a property address under Purchase Flow → Property Estimate:
- The address should auto-save.
- The property/scenario should save to the user’s dashboard.
- The property tab/scenario should remain available after switching tabs.
- The property should remain available after refreshing.
- The property should remain available after logging out and logging back in.
- Zillow/cache data and estimate numbers should also remain tied to that purchase property.

Please compare the Purchase persistence logic against the Refinance persistence logic.

Likely issue:
Refinance is saving to the correct dashboard/user/scenario table consistently, but Purchase may be saving only to local state, temporary session state, or an incomplete scenario object.

Please inspect:
- Where Refinance addresses are saved.
- Where Purchase addresses are saved.
- Whether Purchase and Refinance use different tables, save functions, or dashboard loaders.
- Whether Purchase is missing an upsert after address selection.
- Whether Purchase is creating a scenario locally but not persisting it to Supabase.
- Whether Purchase only saves after a later step, instead of immediately after address selection.
- Whether Purchase records are saved but not loaded back into the dashboard.
- Whether Purchase records are missing a required user ID, flow type, scenario type, address field, or normalized property key.
- Whether Purchase is being filtered out by the dashboard query.
- Whether Purchase scenario IDs are unstable or regenerated on reload.
- Whether adding a new purchase property overwrites or replaces prior purchase properties instead of appending.
- Whether Purchase is using array index instead of stable scenario/property ID.
- Whether Zillow/cache updates are saved but the original purchase scenario record is not.

Fix requirements:
1. Make Purchase property persistence match Refinance persistence.
2. When a logged-in user selects a valid address in Purchase Flow → Property Estimate:
   - Create or upsert a purchase scenario record immediately.
   - Save the full formatted address.
   - Save the normalized property key.
   - Save the stable property/scenario ID.
   - Save the user ID.
   - Save the flow type as purchase.
   - Save default estimate values initially.
3. When Zillow/cache data returns:
   - Update the same purchase scenario record.
   - Do not create a duplicate record.
   - Do not update the wrong property.
4. When the user edits purchase details:
   - Auto-save the updates to the same purchase scenario record.
5. When the dashboard loads:
   - Query and display all saved purchase scenarios for the logged-in user.
   - Use the same reliability pattern that Refinance uses.
6. Do not rely on local component state as the source of truth for saved purchase properties.
7. Do not require “Save to Dashboard.”
8. Do not require “Pull from Zillow.”
9. Do not let purchase records disappear after refresh or navigation.
10. Do not let one purchase property overwrite another purchase property.

Implementation preference:
- Reuse the proven Refinance save/load pattern if possible.
- If Refinance has a stable working function for saving/loading address scenarios, adapt that same pattern for Purchase.
- Keep the smallest safe code change.

Auto-save timing:
- Save purchase address immediately after valid Google Maps selection.
- Save default purchase estimate values immediately after scenario creation.
- Save Zillow/cache results immediately after they are applied.
- Save user edits with a short debounce.
- Show a small save status:
  - “Saving…”
  - “Saved”
  - “Unable to save — retrying”

Acceptance test:
1. Log in.
2. Go to Dashboard → Purchase Flow → Property Estimate.
3. Add a property address.
4. Confirm the purchase property/scenario appears.
5. Refresh the page.
6. Confirm the purchase property is still there.
7. Leave the Purchase tab and go to Refinance.
8. Come back to Purchase.
9. Confirm the purchase property is still there.
10. Log out and log back in.
11. Confirm the purchase property is still there.
12. Add a second purchase property.
13. Confirm both purchase properties persist.
14. Confirm the first purchase property is not overwritten by the second.
15. Confirm Zillow/cache data updates the same saved purchase record.
16. Confirm dashboard purchase list loads all saved purchase properties.
17. Confirm Refinance persistence still works unchanged.

Please tell me exactly:
- Why Refinance was persisting but Purchase was not
- Which save/load functions were different
- Which Purchase save/load logic was fixed
- Which database/table fields were involved
- Which files/functions were changed