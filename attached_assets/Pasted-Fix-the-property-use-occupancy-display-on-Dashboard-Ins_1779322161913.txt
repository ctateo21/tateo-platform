Fix the property use / occupancy display on Dashboard → Insurance overview.

Current issue:
On the Insurance overview, the property use appears twice:
1. A box/badge above that says the property use
2. A dropdown below that also says the same property use

This is redundant and confusing.

Expected behavior:
On Dashboard → Insurance overview, there should only be one property-use control:
- A single dropdown
- Options:
  - Primary
  - Secondary
  - Investment

Remove the extra box/badge above it that repeats the same value.

Property use carryover logic:
The Insurance property use/occupancy should be automatically derived from the matching Purchase or Refinance record when the address matches.

Example:
- Purchase has 3102 W Nassau as Primary
- Insurance has 3102 W Nassau
- Insurance dropdown should automatically show Primary

Example:
- Refinance has 123 Main St as Investment
- Insurance has 123 Main St
- Insurance dropdown should automatically show Investment

Address matching:
Use the normalized property key, not the raw formatted address.

Match by:
- street number
- normalized street name
- unit number, if applicable
- ZIP5
- state

Do not rely on city as the main matching field.

Required behavior:
1. When Insurance overview loads, check each Insurance property address against saved Purchase and Refinance records.
2. If a matching Purchase or Refinance record has occupancy/property use, prefill the Insurance dropdown with that value.
3. Save that carried-over value to the Insurance record.
4. Show only the dropdown — no separate duplicate box/badge above it.
5. The user can change the dropdown if needed.
6. If the user changes it, save that as an Insurance manual override.
7. Once manually overridden, do not keep replacing it from Purchase or Refinance.
8. The Insurance record should remain linked to the matched Purchase/Refinance property by normalized address key or stable linked scenario ID.

Stored values should be consistent:
- `primary`
- `secondary`
- `investment`

Display labels:
- Primary
- Secondary
- Investment

Suggested fields:
- `occupancy_type`
- `occupancy_source`: `"purchase" | "refinance" | "insurance_manual" | "unknown"`
- `normalized_property_key`
- `linked_purchase_scenario_id`
- `linked_refinance_scenario_id`

Priority rules:
1. If Insurance has a manual override, show the manual Insurance value.
2. Else, if a matching Purchase record has property use, use Purchase.
3. Else, if a matching Refinance record has property use, use Refinance.
4. Else, leave the dropdown blank or show “Select property use.”

Important:
- Do not show both a badge/box and dropdown for property use.
- Do not change the Insurance overview layout beyond removing the duplicate display and fixing the dropdown behavior.
- Do not show Purchase or Refinance records as Insurance records.
- Do not overwrite Purchase or Refinance data from the Insurance dropdown.
- Only update the Insurance record when the dropdown is changed.
- Preserve the current Insurance overview view that is working.

Acceptance test:
1. Go to Dashboard → Insurance overview.
2. Confirm each Insurance property has only one property-use dropdown.
3. Confirm the duplicate box/badge above the dropdown is removed.
4. Confirm dropdown options are only Primary, Secondary, and Investment.
5. Add or load an Insurance property whose address matches a Purchase property marked Primary.
6. Confirm Insurance dropdown automatically shows Primary.
7. Add or load an Insurance property whose address matches a Refinance property marked Investment.
8. Confirm Insurance dropdown automatically shows Investment.
9. Manually change the Insurance dropdown.
10. Confirm the manual value saves and is not overwritten by Purchase/Refinance.
11. Refresh/logout/login.
12. Confirm the selected Insurance property use persists.
13. Confirm Purchase and Refinance records are unchanged.

Please make the smallest safe code change possible and tell me:
- Which duplicate property-use UI element was removed
- Which dropdown component is now the single source of truth
- How Purchase/Refinance occupancy is matched and carried over
- How manual Insurance overrides are saved
- Which files/functions were changed