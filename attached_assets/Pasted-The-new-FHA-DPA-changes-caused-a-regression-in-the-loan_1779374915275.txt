The new FHA DPA changes caused a regression in the loan recommendation logic.

Current issue:
When the user answers that their credit score is below 720, the app no longer automatically switches/recommends FHA as the loan option.

Before the DPA code change, this worked correctly:
- Credit score below 720 should automatically make FHA the selected/recommended loan option.
- Everything else with pricing is now working correctly, so do not rewrite the pricing logic broadly.

Expected behavior:
If the user enters a credit score below 720 in the Purchase Flow, the app should automatically select FHA as the loan option, unless the user has manually overridden the loan type after the recommendation was made.

Requirements:
1. Restore the original automatic FHA selection logic for credit scores below 720.
2. Keep all new FHA DPA pricing and toggle logic working.
3. Do not break Conventional pricing.
4. Do not break FHA standard 3.5% down.
5. Do not break FHA DPA repayable or forgivable calculations.
6. Do not break Page 3 / Page 4 sync.
7. Do not break Mortgage News Daily rate selection.

Loan selection logic:
- If credit score is below 720:
  - Automatically select/recommend FHA.
- If credit score is 720 or higher:
  - Use the existing loan recommendation logic.
- If the user manually selects a different loan type after the automatic recommendation:
  - Respect the user’s manual selection unless the app already has different intended behavior.

Likely cause:
The FHA DPA implementation may have changed or bypassed the existing loan recommendation function, credit score watcher, or loan type auto-selection effect.

Please inspect:
- The function/effect that watches credit score changes.
- The function that recommends loan type based on credit score.
- Any condition added for FHA DPA that prevents FHA from being selected.
- Any new default loan type logic added for DPA.
- Whether Page 3 and Page 4 are using different loan type state.
- Whether `loanType`, `selectedLoanType`, `recommendedLoanType`, or similar fields are out of sync.
- Whether manual override logic is incorrectly blocking the initial auto-selection.

Fix requirements:
1. Find the original credit-score-based loan recommendation logic.
2. Restore the rule that credit scores below 720 auto-select FHA.
3. Make FHA DPA options depend on FHA being selected, not the other way around.
4. If FHA is auto-selected due to credit score below 720:
   - default down payment to 3.5%
   - show FHA DPA option
   - keep DPA off unless the user chooses 0% down/DPA
5. If user later chooses FHA DPA:
   - apply the DPA pricing rules as currently working.
6. Keep the loan type selection synced across:
   - Purchase questionnaire
   - Page 3 Purchase Details
   - Page 4 See My Estimate
   - Dashboard saved scenario

Important:
Do not rewrite the FHA DPA pricing because the pricing is working. Only fix the broken auto-selection/recommendation behavior.

Acceptance test:
1. Start a purchase estimate.
2. Enter credit score 719 or below.
3. Confirm loan option automatically becomes FHA.
4. Confirm FHA defaults to 3.5% down.
5. Confirm FHA DPA option is still available but not automatically selected.
6. Select 0% down FHA DPA repayable.
7. Confirm DPA pricing still works.
8. Select 0% down FHA DPA forgivable.
9. Confirm DPA pricing still works.
10. Enter credit score 720 or higher.
11. Confirm the app follows the existing non-FHA recommendation logic.
12. Confirm Page 3 and Page 4 show the same selected loan type.
13. Confirm saved dashboard scenario keeps the correct loan type.

Please make the smallest safe code change possible and tell me:
- Which loan recommendation function/effect was broken
- What DPA code caused the FHA auto-selection regression
- How the below-720 FHA auto-selection was restored
- Which files/functions were changed