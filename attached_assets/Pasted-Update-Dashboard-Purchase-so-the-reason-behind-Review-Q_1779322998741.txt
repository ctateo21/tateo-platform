Update Dashboard → Purchase so the reason behind Review/Qualifies is visually highlighted.

Current issue:
Each Purchase property shows key numbers like:
- Cash to Close
- DTI

The status says either:
- Review
- Qualifies

But the specific number causing that status is not clearly highlighted.

Expected behavior:
Whichever data point is causing the property to flip to Review or Qualifies should be visually highlighted.

Main rule:
- If Cash to Close is the reason the property is marked Review, put Cash to Close in a red box.
- If DTI is the reason the property is marked Review, put DTI in a red box.
- If both Cash to Close and DTI are causing Review, put both in red boxes.
- If the property Qualifies, highlight the qualifying reason/metric clearly, preferably in green.
- Do not randomly color both unless both are actually part of the qualification decision.

Clarify the logic:
1. Find the qualification logic that determines whether the property is Review or Qualifies.
2. Identify which metric(s) trigger that result.
3. Expose those reason codes to the Purchase dashboard row/card.

Suggested reason values:
- `cash_to_close_shortfall`
- `dti_too_high`
- `cash_to_close_ok`
- `dti_ok`
- `both_review`
- `both_qualify`

Dashboard display:
- Cash to Close box:
  - Red if cash to close/assets are the reason for Review.
  - Green if cash to close/assets are part of why the property Qualifies.
  - Neutral if not relevant to the decision.
- DTI box:
  - Red if DTI is the reason for Review.
  - Green if DTI is part of why the property Qualifies.
  - Neutral if not relevant to the decision.

Important:
- Do not change the actual qualification math.
- Do not change the Review/Qualifies decision logic unless it is currently not exposing the reason.
- Only expose/use the existing reason behind the decision.
- Keep the status badge colors:
  - Review = Red
  - Qualifies = Green
- Keep the UI clean and readable.
- Do not change Purchase persistence.
- Do not change Refinance or Insurance.

Acceptance test:
1. Go to Dashboard → Purchase.
2. Find a property marked Review due to Cash to Close.
3. Confirm Cash to Close is in a red box.
4. Find a property marked Review due to DTI.
5. Confirm DTI is in a red box.
6. Find a property marked Review due to both.
7. Confirm both Cash to Close and DTI are in red boxes.
8. Find a property marked Qualifies.
9. Confirm qualifying metrics are shown clearly, preferably in green.
10. Confirm the Review/Qualifies status badge still uses red/green.
11. Confirm no qualification calculations changed.

Please make the smallest safe code change possible and tell me:
- Where the qualification reason is calculated
- Which reason codes were added or reused
- Which Purchase dashboard component was changed
- How Cash to Close and DTI boxes are colored
- Which files/functions were changed