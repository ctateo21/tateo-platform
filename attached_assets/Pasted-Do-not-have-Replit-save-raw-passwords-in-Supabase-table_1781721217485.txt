Do **not** have Replit save raw passwords in Supabase tables. Passwords should be handled by Supabase Auth only. The “save username/password” option should use the browser/mobile password manager behavior, not custom password storage.

Copy/paste this into Replit:

````text id="auth-email-only-remember-password-forgot-password"
Update account creation/login so usernames must be email addresses only, users can save credentials through their browser/mobile password manager, and users can reset forgotten passwords by email.

Important security rule:
Do not store raw passwords in Supabase tables.
Do not create a `password` column.
Do not create a `saved_password` column.
Do not store plaintext passwords anywhere.

Passwords must be handled only by Supabase Auth or the existing auth provider.
Supabase Auth will securely hash and manage passwords internally.

Current request:
When a user creates an account:
1. Username/login should only ever be an email address.
2. User should be able to save the username/password for future login.
3. Add a “Forgot Password?” option that sends a password reset email.
4. Inspect Supabase schema and tell me whether any SQL is needed.

Goal:
- Email is the only allowed username.
- Passwords are managed securely through Supabase Auth.
- Browser/mobile password managers can offer to save the email/password.
- Forgot password flow emails the user a reset link.
- No credit card required.
- Keep free-account flow intact.

PART 1 — Email-only username

Account creation:
- Remove or ignore any separate username field.
- Use email as the only login identifier.
- The signup form should require:
  - Email
  - Phone number
  - Password
  - Confirm password, if existing pattern uses it

Login:
- Login form should use:
  - Email
  - Password

Validation:
- Email field must be a valid email address.
- Do not allow non-email usernames.
- If a user types something that is not an email, show:
  “Please enter a valid email address.”

Labels:
Signup email field:
“Email”

Login email field:
“Email”

Do not use:
- Username
- User ID
- Login name

PART 2 — Save username/password option

Do not build a custom “save password” database feature.

Instead, make the forms compatible with browser and mobile password managers.

Use proper form attributes:

Signup form:
- email input:
  - `type="email"`
  - `name="email"`
  - `autoComplete="email"`
- phone input:
  - `type="tel"`
  - `name="tel"`
  - `autoComplete="tel"`
- password input:
  - `type="password"`
  - `name="new-password"`
  - `autoComplete="new-password"`
- confirm password input:
  - `type="password"`
  - `name="new-password"`
  - `autoComplete="new-password"`

Login form:
- email input:
  - `type="email"`
  - `name="email"`
  - `autoComplete="username"`
- password input:
  - `type="password"`
  - `name="password"`
  - `autoComplete="current-password"`

This lets iPhone, Android, Chrome, Safari, and password managers offer:
- save password
- autofill email/password
- update saved password

Optional UI:
Add a small note near account creation:
“Your browser or phone may ask if you want to save this login.”

Do not add a checkbox that stores the password in our database.

If a “Remember me” checkbox already exists:
- It can remember the session only.
- It must not store the password.
- Label should be:
  “Keep me signed in”
- Use Supabase session persistence or existing auth session persistence.

PART 3 — Forgot password flow

Add “Forgot Password?” link to the login form.

Location:
- Under password field
- Near Log In button

Text:
“Forgot Password?”

When clicked:
1. Show password reset form/modal.
2. Ask for email address.
3. Validate email.
4. Call Supabase Auth password reset email function.
5. Show success message.

Use Supabase method if using Supabase Auth:
`supabase.auth.resetPasswordForEmail(email, { redirectTo })`

Redirect URL:
Use the deployed app URL, not localhost.

Suggested redirect:
`${PUBLIC_SITE_URL}/reset-password`

or existing reset route if already present.

Success message:
“If an account exists for this email, a password reset link has been sent.”

Use this wording so we do not reveal whether an email exists.

PART 4 — Reset password page

Create or confirm a reset password route/page exists.

Suggested route:
`/reset-password`

Behavior:
1. User clicks reset link from email.
2. App opens reset password page.
3. User enters new password.
4. User confirms new password.
5. App calls Supabase Auth update user password:
   `supabase.auth.updateUser({ password: newPassword })`
6. Show success:
   “Your password has been updated. You can now log in.”
7. Redirect to login or dashboard.

Validation:
- password required
- confirm password must match
- minimum length should match current auth rules
- show clean error messages

Reset password inputs:
- new password:
  - `type="password"`
  - `name="new-password"`
  - `autoComplete="new-password"`
- confirm new password:
  - `type="password"`
  - `name="new-password"`
  - `autoComplete="new-password"`

PART 5 — Supabase / SQL requirement

Inspect the existing Supabase schema first.

Important:
Do not add SQL for saving passwords.
Passwords belong in Supabase Auth, not public tables.

Check whether the app already has a profile table:
- `profiles`
- `user_profiles`
- `users`
- any equivalent public user profile table

Required profile data:
- auth user id
- email
- phone
- created_at
- updated_at
- last_sign_in_at, if used
- fub_contact_id, if using Follow Up Boss

If all required profile fields already exist:
- do not create new SQL
- reuse existing columns
- confirm no SQL was needed

If profile fields are missing:
- provide one complete SQL migration
- do not add password fields
- do not weaken RLS

Suggested SQL only if needed:

```sql
create table if not exists public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  email text,
  phone text,
  fub_contact_id text,
  signup_source text,
  first_address_searched text,
  last_sign_in_at timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

alter table public.profiles
  add column if not exists email text,
  add column if not exists phone text,
  add column if not exists fub_contact_id text,
  add column if not exists signup_source text,
  add column if not exists first_address_searched text,
  add column if not exists last_sign_in_at timestamptz,
  add column if not exists created_at timestamptz default now(),
  add column if not exists updated_at timestamptz default now();

alter table public.profiles enable row level security;

create policy if not exists "Users can view own profile"
on public.profiles
for select
using (auth.uid() = id);

create policy if not exists "Users can insert own profile"
on public.profiles
for insert
with check (auth.uid() = id);

create policy if not exists "Users can update own profile"
on public.profiles
for update
using (auth.uid() = id)
with check (auth.uid() = id);
````

If an existing profiles table already has policies:

* do not duplicate conflicting policies
* inspect first
* only add missing columns if needed

PART 6 — Auth/session behavior

Signup:

* Create user with Supabase Auth email/password.
* Store email and phone in profile/user metadata.
* Do not store password in profile.
* Keep existing FUB account-created notification.

Login:

* Login with Supabase Auth email/password.
* Notify FUB on actual successful sign-in action.
* Do not notify FUB on simple session restore/page refresh.
* Update `last_sign_in_at` in profile if available.

Session persistence:

* Keep user signed in based on Supabase Auth/session settings.
* Do not build custom password storage.
* Let browser/mobile password manager handle saved credentials.

PART 7 — UI copy

Signup page/modal:
Title:
“Create a free account”

Fields:

* Email
* Phone
* Password
* Confirm Password

Small helper text:
“Your browser or phone may ask if you want to save this login.”

Login page/modal:
Fields:

* Email
* Password

Links:

* Forgot Password?
* Create a free account

Forgot password page/modal:
Title:
“Reset your password”

Text:
“Enter your email and we’ll send you a password reset link.”

Button:
“Send Reset Link”

Success:
“If an account exists for this email, a password reset link has been sent.”

Reset password page:
Title:
“Create a new password”

Button:
“Update Password”

PART 8 — Do not break

Do not break:

* free account creation
* phone requirement
* email/password login
* Save Scenario auth gate
* Share auth gate
* Download PDF auth gate
* dashboard access
* Stripe bypass/free access mode
* Follow Up Boss account-created notification
* Follow Up Boss sign-in notification
* guest one-time estimate flow
* saved scenarios
* Supabase RLS

PART 9 — Debug logs

Add temporary logs:

Signup:

* `[auth-signup] email valid`
* `[auth-signup] phone present`
* `[auth-signup] supabase user created`
* `[auth-signup] profile saved`
* `[auth-signup] password not stored in public tables`

Login:

* `[auth-login] email login attempted`
* `[auth-login] success`
* `[auth-login] fub sign-in notification sent`

Forgot password:

* `[auth-forgot-password] reset requested`
* `[auth-forgot-password] email sent or generic success`
* `[auth-forgot-password] error`

Reset password:

* `[auth-reset-password] update started`
* `[auth-reset-password] update success`
* `[auth-reset-password] update error`

SQL/schema:

* `[auth-schema] profile table found`
* `[auth-schema] missing fields`
* `[auth-schema] sql needed yes/no`

PART 10 — Acceptance tests

Test A — Email-only signup

1. Open signup.
2. Try entering a non-email username.
3. Confirm form rejects it.
4. Enter valid email, phone, and password.
5. Confirm account creates successfully.
6. Confirm password is not stored in public profiles table.

Test B — Browser password save

1. Create account in Chrome/Safari/mobile browser.
2. Confirm browser/password manager offers to save login.
3. Log out.
4. Return to login.
5. Confirm email/password can autofill.

Test C — Login

1. Login with email and password.
2. Confirm login succeeds.
3. Confirm FUB sign-in notification fires only on actual sign-in.

Test D — Forgot password

1. Click Forgot Password.
2. Enter email.
3. Confirm reset email is sent/generic success message appears.
4. Click reset link.
5. Enter new password.
6. Confirm password updates.
7. Login with new password.

Test E — SQL

1. Inspect Supabase schema.
2. Confirm whether SQL was needed.
3. Confirm no password columns were created.
4. Confirm profile table stores email/phone only.
5. Confirm RLS remains secure.

Please make the smallest safe code change possible and tell me:

* Whether SQL was needed
* Which SQL migration was added, if any
* Confirmation that passwords are not stored in public tables
* How email-only username validation works
* How browser/mobile password saving is supported
* How Forgot Password works
* Which reset URL is used
* How phone is stored
* How FUB signup/sign-in notifications are preserved
* Which files/functions were changed

```
```
