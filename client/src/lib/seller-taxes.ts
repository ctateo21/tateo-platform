// Estimated taxes due when selling a home.
//
// PLACEHOLDER: currently always returns 0 (display-only). When a real
// calculation is ready, implement it here and every call site — the
// Sell Your Home overview cards and the detailed view — will pick it up.
export function getEstimatedSellerTaxesDue(_sellerScenario?: unknown): number {
  return 0;
}
